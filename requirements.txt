# STEP File Analyzer Requirements
# Install with: pip install -r requirements.txt

# HTTP client for Ollama API
requests>=2.31.0

# Core OpenCASCADE binding for geometric analysis.
# Keep major version constrained, allow solver to pick a Python-compatible wheel.
cadquery-ocp>=7.8,<8.0

# Scientific computing and visualization (required by cadquery-ocp)
numpy>=1.23.0
matplotlib>=3.0.0
# VTK wheel availability differs by Python ABI:
# - py<3.13: 9.3.1 is available and stable for local Windows setup.
# - py>=3.13 (Streamlit Cloud default): use newer VTK wheels.
vtk==9.3.1; python_version < "3.13"
vtk>=9.5.0,<9.7; python_version >= "3.13"

# Additional utilities
pathlib2>=2.3.0; python_version < "3.4"

# Web UI
streamlit>=1.37.0
pyinstaller>=6.6.0

# Optional: For enhanced error reporting and debugging
# traceback is built-in to Python

# Development dependencies (optional)
# jupyter>=1.0.0
# notebook>=6.0.0
# ipykernel>=6.0.0
